package api

import (
	"net/http"
)

func (a *RegistryAPI) StatusHandler(w http.ResponseWriter, r *http.Request) {
	// TODO[jigish] implement this
	// informative data about the server should go here =P
	NotImplementedHandler(w, r)
}
